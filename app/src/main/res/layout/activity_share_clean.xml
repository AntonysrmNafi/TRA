<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately empty and invisible: this screen exists only so the app
     can appear as a Share-sheet target. It should never visibly render
     anything — see ShareCleanActivity for why. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
